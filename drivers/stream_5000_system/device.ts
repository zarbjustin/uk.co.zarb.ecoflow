'use strict';

import { Stream5000UnitDevice } from '../../lib/Stream5000UnitDevice';

/** Installation aggregate: the single STREAM 5000 contribution to Homey Energy. */
module.exports = Stream5000UnitDevice;
